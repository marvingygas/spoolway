<!--
  The docs directory's front page. One per project, not one per domain, so it
  carries no `domain`/`covers` frontmatter — nothing routes changes to it, and
  it is rewritten whenever the set of documents around it changes.

  Its job is orientation: what this project is, and where to go next. Anything
  that belongs to one domain belongs in that domain's document instead.
-->

# <Project>

<One sentence: what this project is and who it is for.>

## Start here

The shortest path from nothing to a working setup. Installing it, running it
once, and knowing the run succeeded.

## Documents

Every domain document, with a line each on what it covers. A reader should be
able to pick one from this table without opening two of them first.

| Document | What it covers |
| --- | --- |

## How the pieces fit

How the domains above relate to each other — what calls what, what owns what.
A diagram earns its place here more often than it does inside a single
document, because this is the only page that sees all of them at once.

## Reference

The project-wide references that belong to no single domain.

## Getting help

Where questions go, and what to include when asking.
