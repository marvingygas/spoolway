# Commits that changed layout and nothing else. `git blame` skips these, so it
# points at whoever last changed the code rather than at the reformat.
#
# Enable it once per clone:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file on its own and needs no configuring.

# Run rustfmt
f993d0339c37a2495db4ed97c64a2f89f65a0649
